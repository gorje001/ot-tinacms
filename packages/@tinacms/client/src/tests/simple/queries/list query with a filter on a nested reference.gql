query {
  posts: postConnection(
    filter: { author: { author: { name: { eq: "Pedro" } } } }
  ) {
    edges {
      node {
        ...SystemInfo
        title
        author {
          ... on Document {
            id
          }
        }
      }
    }
  }
}
